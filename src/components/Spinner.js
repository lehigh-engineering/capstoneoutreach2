// Spinner.js
import React from 'react';
import './Spinner.css'; // Make sure to add the CSS file for styling

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;